// dont mind... will edit in the future

const User = require(path.join(__dirname, '..', 'models', 'user'));
const router = require("express").Router();


// Update user
// Delete user
// Get a user
// Follow a User
// Unfollow a User

module.exports = router;